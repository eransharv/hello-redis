StackExchange.Redis (.NET) client code example, how to connect to redis labs resource.

Download all the files and open the visual studio solution file (sln).

The solution contains reference to the StackExchange.Redis dll, using nuget package.